from sqlalchemy import Column, Integer, String, DateTime, create_engine\nfrom sqlalchemy.ext.declarative import declarative_base\nfrom sqlalchemy.orm import sessionmaker\n\nBase = declarative_base()\n\nclass VerificationLog(Base):\n    __tablename__ = 'verification_logs'\n\n    id = Column(Integer, primary_key=True, autoincrement=True)\n    user_id = Column(String(50), nullable=False)\n    verification_status = Column(String(50), nullable=False)\n    timestamp = Column(DateTime, nullable=False)\n    details = Column(String(255))\n\n    def __repr__(self):\n        return f'<VerificationLog(id={self.id}, user_id={self.user_id}, verification_status={self.verification_status}, timestamp={self.timestamp}, details={self.details})>'\n\n# Database setup (example)\n# engine = create_engine('sqlite:///verification_logs.db')\n# Base.metadata.create_all(engine)\n\n